{
  "name": "@html-poker/cloudflare-connection-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0"
  }
}
